<article class="dashboard">
  <h1>Dashboard</h1>
  <% if @forms.any? %>
    <ul class="forms-list">
    <% @forms.each do |f| %>
      <li class="form-card"><%= f.name %></li>
    <% end %>
    </ul>
  <% end %>
</article>